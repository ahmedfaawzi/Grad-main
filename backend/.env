# KMS Settings
KMS_KEY_ID=alias/library-db-credentials

# Flask Settings
SECRET_KEY=grad-main-library-system-2024-secret
DEBUG=True
HOST=0.0.0.0
PORT=5000
FLASK_ENV=development

# Note: Database credentials are now stored in encrypted_credentials.json
# using AWS KMS for security
