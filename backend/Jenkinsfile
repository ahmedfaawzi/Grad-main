pipeline {
    agent any

    stages {

        stage('Checkout Code') {
            steps {
                checkout scm
            }
        }

        stage('Build & Run with Docker Compose') {
            steps {
                sh '''
                  cd ..
                  sudo docker compose down || true
                  sudo docker compose up -d --build
                '''
            }
        }
    }
}
